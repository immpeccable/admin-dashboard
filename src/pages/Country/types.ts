import { I_APPLICATION_COUNT } from "../../components/Header/types";

export type HoverCardProps = {
  name: string;
  positionX: number;
  positionY: number;
  waitingApplications: I_APPLICATION_COUNT;
};
